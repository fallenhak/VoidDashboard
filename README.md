# VoidDashboard

This branch stores the GitHub Actions workflow that deploys the built dashboard from the `gh-pages` branch.

Dashboard files are published to `gh-pages` from the private `fallenhak/Void` source repository.